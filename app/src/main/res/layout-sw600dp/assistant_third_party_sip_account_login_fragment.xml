<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <import type="android.text.InputType" />
        <variable
            name="backClickListener"
            type="View.OnClickListener" />
        <variable
            name="viewModel"
            type="org.linphone.ui.assistant.viewmodel.ThirdPartySipAccountLoginViewModel" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/color_main2_000">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="40dp">

                <!-- VoIP PHONE Logo Section -->
                <ImageView
                    android:id="@+id/voip_logo"
                    android:layout_width="120dp"
                    android:layout_height="120dp"
                    android:layout_marginTop="60dp"
                    android:src="@mipmap/ic_launcher"
                    android:contentDescription="VoIP Phone Logo"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/voip_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="20dp"
                    android:text="VoIP PHONE"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:textColor="#2196F3"
                    android:letterSpacing="0.1"
                    app:layout_constraintTop_toBottomOf="@id/voip_logo"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />


                <!-- Email Field with Icon -->
                <androidx.appcompat.widget.AppCompatEditText
                    android:id="@+id/username"
                    android:layout_width="400dp"
                    android:layout_height="60dp"
                    android:layout_marginTop="20dp"
                    android:paddingStart="52dp"
                    android:paddingEnd="16dp"
                    android:text="@={viewModel.username, default=`nasriwahid90@gmail.com`}"
                    android:textSize="18sp"
                    android:textColor="#333333"
                    android:background="@drawable/edit_text_background"
                    android:inputType="textEmailAddress"
                    android:hint="Email"
                    app:layout_constraintTop_toBottomOf="@id/voip_title"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />

                <ImageView
                    android:id="@+id/email_icon"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:src="@drawable/envelope_simple"
                    android:contentDescription="Email icon"
                    app:tint="#2196F3"
                    app:layout_constraintStart_toStartOf="@id/username"
                    app:layout_constraintTop_toTopOf="@id/username"
                    app:layout_constraintBottom_toBottomOf="@id/username"
                    android:layout_marginStart="16dp" />

                <!-- Password Field with Icons -->
                <androidx.appcompat.widget.AppCompatEditText
                    android:id="@+id/password"
                    android:layout_width="400dp"
                    android:layout_height="60dp"
                    android:layout_marginTop="20dp"
                    android:paddingStart="52dp"
                    android:paddingEnd="52dp"
                    android:text="@={viewModel.password, default=`password123`}"
                    android:textSize="18sp"
                    android:textColor="#333333"
                    android:background="@drawable/edit_text_background"
                    android:hint="Password"
                    passwordInputType="@{viewModel.showPassword ? InputType.TYPE_CLASS_TEXT | InputType.TYPE_TEXT_VARIATION_VISIBLE_PASSWORD : InputType.TYPE_CLASS_TEXT | InputType.TYPE_TEXT_VARIATION_PASSWORD, default=textPassword}"
                    app:layout_constraintTop_toBottomOf="@id/username"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />

                <ImageView
                    android:id="@+id/lock_icon"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:src="@drawable/lock_simple"
                    android:contentDescription="Lock icon"
                    app:tint="#2196F3"
                    app:layout_constraintStart_toStartOf="@id/password"
                    app:layout_constraintTop_toTopOf="@id/password"
                    app:layout_constraintBottom_toBottomOf="@id/password"
                    android:layout_marginStart="16dp" />

                <ImageView
                    android:onClick="@{() -> viewModel.toggleShowPassword()}"
                    android:id="@+id/eye"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:padding="2dp"
                    android:contentDescription="@string/content_description_toggle_password_visibility"
                    android:src="@{viewModel.showPassword ? @drawable/eye_slash : @drawable/eye, default=@drawable/eye}"
                    app:tint="#2196F3"
                    app:layout_constraintEnd_toEndOf="@id/password"
                    app:layout_constraintTop_toTopOf="@id/password"
                    app:layout_constraintBottom_toBottomOf="@id/password"
                    android:layout_marginEnd="16dp" />

                <!-- Login Button -->
                <androidx.appcompat.widget.AppCompatTextView
                    android:onClick="@{() -> viewModel.login()}"
                    android:enabled="@{viewModel.loginEnabled &amp;&amp; !viewModel.registrationInProgress, default=true}"
                    android:id="@+id/login"
                    android:layout_width="400dp"
                    android:layout_height="60dp"
                    android:layout_marginTop="50dp"
                    android:layout_marginBottom="50dp"
                    android:gravity="center"
                    android:text="Login"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:textColor="#FFFFFF"
                    android:background="@drawable/primary_button_background"
                    app:layout_constraintTop_toBottomOf="@id/password"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />

            </androidx.constraintlayout.widget.ConstraintLayout>

        </ScrollView>

        <include
            layout="@layout/operation_in_progress"
            bind:visibility="@{viewModel.registrationInProgress}" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>