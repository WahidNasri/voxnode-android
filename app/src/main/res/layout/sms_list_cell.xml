<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- SMS Status Icon -->
        <ImageView
            android:id="@+id/sms_status_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/check"
            app:tint="?attr/color_main1_500"
            android:contentDescription="@null"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Recipient Number -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/recipient_number"
            style="@style/default_text_style_600"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:textColor="?attr/color_main2_600"
            android:textSize="16sp"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintEnd_toStartOf="@+id/timestamp"
            app:layout_constraintStart_toEndOf="@+id/sms_status_icon"
            app:layout_constraintTop_toTopOf="@+id/sms_status_icon"
            tools:text="+33123456789" />

        <!-- Timestamp -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/timestamp"
            style="@style/default_text_style"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?attr/color_main2_400"
            android:textSize="12sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/sms_status_icon"
            tools:text="10:30" />

        <!-- SMS Content -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/sms_content"
            style="@style/default_text_style"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:textColor="?attr/color_main2_500"
            android:textSize="14sp"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/sms_status_icon"
            app:layout_constraintTop_toBottomOf="@+id/recipient_number"
            tools:text="This is a sample SMS message content that might be quite long and will be truncated..." />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="16dp"
        android:background="?attr/color_main2_200" />

</LinearLayout>
